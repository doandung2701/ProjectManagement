import { BluronfocusoutDirective } from './bluronfocusout.directive';

describe('BluronfocusoutDirective', () => {
  it('should create an instance', () => {
    const directive = new BluronfocusoutDirective();
    expect(directive).toBeTruthy();
  });
});
